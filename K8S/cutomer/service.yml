apiVersion: v1
kind: Service

metadata:
  name: Customer-api-service
  labels:
    app: Customer-api-service
    env: non-prod

spec:
  selector:
    app: Customer-api
    env: non-prod
  
  ports:
    -protocol: TCP
    port: 80
    targetPort: 8080



